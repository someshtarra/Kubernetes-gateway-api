---
# ==============================================================================
# NetworkPolicy for Backend v1
# Implements zero-trust isolation: only allows ingress traffic from Envoy Proxy pods
# ==============================================================================
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: backend-v1-allow-envoy
  namespace: production-apps
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: backend
      app.kubernetes.io/instance: backend-v1
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        # Allow ingress from Envoy Proxy data plane in gateway-infra namespace
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/part-of: ingress-platform
          podSelector:
            matchLabels:
              app.gateway.envoyproxy.io/name: envoy
      ports:
        - protocol: TCP
          port: 3000
  egress:
    # Allow DNS resolution to kube-system CoreDNS
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
